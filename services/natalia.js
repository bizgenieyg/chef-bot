// Единый источник правды для номеров, связанных с Натали.
// НЕ дублировать эти строки по файлам — импортировать отсюда.

// Личный номер Натали — сюда уходят все уведомления/эскалации/вопросы,
// адресованные ей как человеку.
const NATALIA_PERSONAL_NUMBER = '972587958060@c.us';

// Номер, под которым авторизована WAHA-сессия самого бота (совпадает с
// личным номером Натали в WhatsApp, но это ДРУГОЙ канал — self-chat бота,
// а не адрес для уведомлений). Используется только для игнор-листа входящих.
const BOT_SESSION_NUMBER = '972559598952@c.us';

// Оба номера, с которых сообщения обрабатываются как "от Натали"
// (а не как заказ от клиента).
const NATALIA_NUMBERS = [NATALIA_PERSONAL_NUMBER, BOT_SESSION_NUMBER];

module.exports = { NATALIA_PERSONAL_NUMBER, BOT_SESSION_NUMBER, NATALIA_NUMBERS };
